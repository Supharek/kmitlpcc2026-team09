# CLAUDE.md

กติกาทั้งหมดของ repo นี้อยู่ใน `AGENTS.md` — อ่านและปฏิบัติตามไฟล์นั้น

@AGENTS.md

## หมายเหตุเฉพาะ Claude Code
- ใช้ plan mode ก่อนลงมือแก้โค้ดที่แตะเกิน 3 ไฟล์
- คำสั่งของโปรเจกต์: `/spec-check` · `/design` · `/bootstrap` · `/next <role>` (พิมพ์ `/` เพื่อดูรายการ)
- ถ้าถูกขอให้ทำงานนอกขอบเขตบทบาทของผู้ใช้ (ตาราง §4 ใน AGENTS.md) ให้ทักท้วงก่อน
