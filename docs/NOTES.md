# NOTES.md — คำถามค้าง + การตัดสินใจ

> ✍️ ทั้งทีมเขียน และ **AI Agent ก็ถูกสั่งให้เขียนที่นี่** เมื่อเจอจุดที่สเปกไม่ชัด
> ไฟล์นี้คือหลักฐานว่าทีม "คิด" ไม่ใช่แค่ "รับของจาก agent"

---

# ส่วนที่ 1 · คำถามที่ยังไม่มีคำตอบ

เจอจุดที่สเปกไม่ชัด **อย่าเดา** — จดที่นี่ ใช้สมมติฐานไปพลางก่อน แล้วทำส่วนที่ชัดเจนต่อ

| # | คำถาม | ถามใคร | สมมติฐานที่ใช้ไปก่อน | สถานะ |
|---|---|---|---|---|
| Q1 | *(ตัวอย่าง)* ถ้าผู้ใช้ส่งฟอร์มซ้ำด้วยอีเมลเดิม ต้องกันไหม หรือให้บันทึกซ้ำได้ | Instructor | บันทึกซ้ำได้ ไม่กัน | ⬜ รอตอบ |
| Q2 | | | | |

---

# ส่วนที่ 2 · การตัดสินใจ (ADR)

จดทุกครั้งที่เลือกทางใดทางหนึ่ง **โดยเฉพาะตอนที่ไม่เห็นด้วยกับ AI Agent**
เพื่อไม่ให้ agent เสนอทางเดิมซ้ำ

## ADR-001 · ใช้ Supabase ผ่าน HTTPS แทนต่อ Postgres ตรง

- **วันที่:** 2026-09-06 · **สถานะ:** กำหนดโดย Instructor
- **บริบท:** Vercel เป็น serverless ทุก request อาจเกิด instance ใหม่ ถ้าต่อ TCP พอร์ต 5432 จะเจอ `too many connections`
- **ทางเลือก:** (1) MySQL — ไม่มี managed free tier ที่ใช้ได้จริง (2) Postgres ผ่าน pooler พอร์ต 6543 — ใช้ได้แต่ต้องตั้งค่าเพิ่ม (3) `supabase-js` ผ่าน HTTPS — ไม่มีเรื่อง connection เลย
- **ตัดสินใจ:** ข้อ 3
- **ผลที่ตามมา:** ไม่ต้องจัดการ connection pool, QA เปิด Supabase Studio ส่องข้อมูลได้เอง, แลกกับการผูกกับ Supabase SDK

## ADR-002 · TODO

- **วันที่:** · **บริบท:** · **ทางเลือกที่พิจารณา:**
- **ตัดสินใจ:** · **เหตุผลที่ไม่เอาอีกทาง:** · **ผลที่ตามมา:**

---

# ส่วนที่ 3 · agent ทำอะไรผิดบ้าง

บันทึกไว้เป็นบทเรียนของทีม — **นี่คือคะแนน ไม่ใช่ความผิด**

| # | สั่งอะไรไป | agent ทำอะไรผิด | จับได้ยังไง | แก้ยังไง |
|---|---|---|---|---|
| 1 | สร้าง Zod schema ใน `lib/validations/lead.ts` | ลืมแปลง `undefined` เป็น `null` ใน `.transform()` ทำให้ field optional คืน undefined | รัน `vitest` แล้ว TC05 ไม่ผ่าน (`expected undefined to be null`) | เพิ่มเงื่อนไข `val === "" || val === undefined ? null : val` |
| 2 | สร้างหน้า `app/page.tsx` | ใส่ข้อความ `// โซลูชันระดับมืออาชีพ` ตรง ๆ ใน JSX children | รัน `next lint` แล้วติดกฎ `react/jsx-no-comment-textnodes` | ครอบข้อความด้วยเครื่องหมายปีกกา `{"// โซลูชันระดับมืออาชีพ"}` |
| 3 | สร้าง `lib/supabase.ts` | เรียก `createClient("", "")` โดยไม่มี fallback เมื่อยังไม่ได้เซ็ต `.env.local` | รัน `next build` แล้ว throw `Error: supabaseUrl is required.` | กำหนดค่า fallback placeholder URL/key เพื่อให้ build ผ่านได้แม้ยังไม่ต่อ Supabase จริง |
