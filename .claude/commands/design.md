---
description: แปลง SPEC.md เป็น DESIGN.md (ER + API) และ TASKS.md
---

อ่าน `docs/SPEC.md` (ทั้งส่วนสเปกและส่วน wireframe) กับ `docs/BRIEF.md`

**หยุดทันทีถ้า:**
- `docs/SPEC.md` ยังมีคำว่า `TODO` เหลืออยู่
- ตาราง `SPEC.md` §2.2 (หน้าไหนตอบ G ข้อไหน) ยังไม่ถูกกรอก

บอกผู้ใช้ว่าต้องเติมตรงไหนก่อน แล้วรอ

> **สำคัญ:** แต่ละทีมออกแบบ sitemap เองไม่เหมือนกัน
> **ห้ามสมมติว่ามีหน้า `/about` `/services` `/contact`** — ใช้เฉพาะหน้าที่อยู่ใน `SPEC.md` §2.1 จริง ๆ

## 1. เขียน `docs/DESIGN.md`

**ส่วนที่ 1 — ฐานข้อมูล**
- ⚠️ **ชื่อตารางทุกตารางต้องขึ้นต้นด้วย `tNN_`** โดย `NN` คือเลขทีมจากชื่อ repo
  (เช่น `kmitlpcc2026-team06` → `t06_contacts`) ทุกทีมใช้ฐานข้อมูลร่วมกัน prefix คือสิ่งเดียวที่กันชนกัน
- Mermaid `erDiagram` ครอบคลุมทุกข้อมูลใน `SPEC.md` §4
- ตารางอธิบายทุกคอลัมน์: ชนิด, null ได้ไหม, default, ความหมาย
- ระบุ index และนโยบาย RLS ของแต่ละตาราง
- สร้างไฟล์ `db/migrations/001_init.sql` ให้ด้วย (ถ้ายังไม่มี)

**ส่วนที่ 2 — ข้อตกลงเรื่อง API**
- ทุก endpoint ที่ `SPEC.md` ต้องใช้ ระบุ method, path, request, response, status code, ตัวอย่างจริง
- ใช้รูปแบบ `{ ok, data }` / `{ ok, error }` ตามที่มีอยู่เดิมในไฟล์
- ข้อความ error ต้องเป็นภาษาไทยที่แสดงให้ผู้ใช้เห็นได้เลย

## 2. เขียน `docs/TASKS.md` ส่วนที่ 1 รอบที่ 2 เป็นต้นไป

- **แตก task ตาม sitemap จริงของทีมใน `SPEC.md` §2.1 — 1 หน้า = อย่างน้อย 1 task**
  ทีมที่มี 3 หน้ากับทีมที่มี 6 หน้า ต้องได้จำนวน task ไม่เท่ากัน
- แตกเป็น task ที่ทำเสร็จได้ใน **20–30 นาที** และตรวจได้ว่าเสร็จจริง
- ระบุบทบาทผู้รับผิดชอบตามตาราง §4 ใน `AGENTS.md`
- ระบุ dependency ให้ชัด เช่น *(รอ T13)*
- จัดกลุ่มเป็นรอบ ให้ **งานที่ทำพร้อมกันได้อยู่รอบเดียวกัน** — ทีมมี 5 คนทำพร้อมกัน
- ปิดท้ายด้วย task ตรวจว่า G1–G4 และ T1–T6 ครบจริง

## ข้อควรระวัง
- อย่าคิดฟีเจอร์ที่ `SPEC.md` ไม่ได้เขียนไว้ ถ้าเห็นว่าจำเป็นให้จดลง `docs/NOTES.md` ส่วนที่ 1 แทน
- อย่าเขียนโค้ดในขั้นนี้ นอกจากไฟล์ `.sql`

**ก่อนเขียนไฟล์ ให้สรุปโครงร่างมาให้ดูก่อน แล้วรอยืนยัน**
